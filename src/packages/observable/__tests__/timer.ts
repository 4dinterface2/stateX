import Record from "../src/record";
import manager from "../src/core/manager";

describe("List tests", done => {
  test("push/pop", done => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1000);
  });
});
